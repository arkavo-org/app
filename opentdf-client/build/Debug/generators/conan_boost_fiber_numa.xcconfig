// Conan XcodeDeps generated file for boost::fiber_numa
// Includes all configurations for each dependency

#include "conan_boost_fiber_numa_debug_arm64.xcconfig"

SYSTEM_HEADER_SEARCH_PATHS = $(inherited) $(SYSTEM_HEADER_SEARCH_PATHS_boost_fiber_numa)
GCC_PREPROCESSOR_DEFINITIONS = $(inherited) $(GCC_PREPROCESSOR_DEFINITIONS_boost_fiber_numa)
OTHER_CFLAGS = $(inherited) $(OTHER_CFLAGS_boost_fiber_numa)
OTHER_CPLUSPLUSFLAGS = $(inherited) $(OTHER_CPLUSPLUSFLAGS_boost_fiber_numa)
FRAMEWORK_SEARCH_PATHS = $(inherited) $(FRAMEWORK_SEARCH_PATHS_boost_fiber_numa)

// Link options for boost_fiber_numa
LIBRARY_SEARCH_PATHS = $(inherited) $(LIBRARY_SEARCH_PATHS_boost_fiber_numa)
OTHER_LDFLAGS = $(inherited) $(OTHER_LDFLAGS_boost_fiber_numa)