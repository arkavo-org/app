// Conan XcodeDeps generated file for boost::type_erasure
// Includes all configurations for each dependency

#include "conan_boost_type_erasure_debug_arm64.xcconfig"

SYSTEM_HEADER_SEARCH_PATHS = $(inherited) $(SYSTEM_HEADER_SEARCH_PATHS_boost_type_erasure)
GCC_PREPROCESSOR_DEFINITIONS = $(inherited) $(GCC_PREPROCESSOR_DEFINITIONS_boost_type_erasure)
OTHER_CFLAGS = $(inherited) $(OTHER_CFLAGS_boost_type_erasure)
OTHER_CPLUSPLUSFLAGS = $(inherited) $(OTHER_CPLUSPLUSFLAGS_boost_type_erasure)
FRAMEWORK_SEARCH_PATHS = $(inherited) $(FRAMEWORK_SEARCH_PATHS_boost_type_erasure)

// Link options for boost_type_erasure
LIBRARY_SEARCH_PATHS = $(inherited) $(LIBRARY_SEARCH_PATHS_boost_type_erasure)
OTHER_LDFLAGS = $(inherited) $(OTHER_LDFLAGS_boost_type_erasure)