// Conan XcodeDeps generated file for boost::dynamic_linking
// Includes all configurations for each dependency

#include "conan_boost_dynamic_linking_release_arm64.xcconfig"

SYSTEM_HEADER_SEARCH_PATHS = $(inherited) $(SYSTEM_HEADER_SEARCH_PATHS_boost_dynamic_linking)
GCC_PREPROCESSOR_DEFINITIONS = $(inherited) $(GCC_PREPROCESSOR_DEFINITIONS_boost_dynamic_linking)
OTHER_CFLAGS = $(inherited) $(OTHER_CFLAGS_boost_dynamic_linking)
OTHER_CPLUSPLUSFLAGS = $(inherited) $(OTHER_CPLUSPLUSFLAGS_boost_dynamic_linking)
FRAMEWORK_SEARCH_PATHS = $(inherited) $(FRAMEWORK_SEARCH_PATHS_boost_dynamic_linking)

// Link options for boost_dynamic_linking
LIBRARY_SEARCH_PATHS = $(inherited) $(LIBRARY_SEARCH_PATHS_boost_dynamic_linking)
OTHER_LDFLAGS = $(inherited) $(OTHER_LDFLAGS_boost_dynamic_linking)