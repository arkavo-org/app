// Conan XcodeDeps generated file for boost::atomic
// Includes all configurations for each dependency

#include "conan_boost_atomic_release_arm64.xcconfig"

SYSTEM_HEADER_SEARCH_PATHS = $(inherited) $(SYSTEM_HEADER_SEARCH_PATHS_boost_atomic)
GCC_PREPROCESSOR_DEFINITIONS = $(inherited) $(GCC_PREPROCESSOR_DEFINITIONS_boost_atomic)
OTHER_CFLAGS = $(inherited) $(OTHER_CFLAGS_boost_atomic)
OTHER_CPLUSPLUSFLAGS = $(inherited) $(OTHER_CPLUSPLUSFLAGS_boost_atomic)
FRAMEWORK_SEARCH_PATHS = $(inherited) $(FRAMEWORK_SEARCH_PATHS_boost_atomic)

// Link options for boost_atomic
LIBRARY_SEARCH_PATHS = $(inherited) $(LIBRARY_SEARCH_PATHS_boost_atomic)
OTHER_LDFLAGS = $(inherited) $(OTHER_LDFLAGS_boost_atomic)