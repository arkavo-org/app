// Conan XcodeDeps generated file for boost::diagnostic_definitions
// Includes all configurations for each dependency

#include "conan_boost_diagnostic_definitions_release_arm64.xcconfig"

SYSTEM_HEADER_SEARCH_PATHS = $(inherited) $(SYSTEM_HEADER_SEARCH_PATHS_boost_diagnostic_definitions)
GCC_PREPROCESSOR_DEFINITIONS = $(inherited) $(GCC_PREPROCESSOR_DEFINITIONS_boost_diagnostic_definitions)
OTHER_CFLAGS = $(inherited) $(OTHER_CFLAGS_boost_diagnostic_definitions)
OTHER_CPLUSPLUSFLAGS = $(inherited) $(OTHER_CPLUSPLUSFLAGS_boost_diagnostic_definitions)
FRAMEWORK_SEARCH_PATHS = $(inherited) $(FRAMEWORK_SEARCH_PATHS_boost_diagnostic_definitions)

// Link options for boost_diagnostic_definitions
LIBRARY_SEARCH_PATHS = $(inherited) $(LIBRARY_SEARCH_PATHS_boost_diagnostic_definitions)
OTHER_LDFLAGS = $(inherited) $(OTHER_LDFLAGS_boost_diagnostic_definitions)