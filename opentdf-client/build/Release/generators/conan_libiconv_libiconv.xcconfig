// Conan XcodeDeps generated file for libiconv::libiconv
// Includes all configurations for each dependency

#include "conan_libiconv_libiconv_release_arm64.xcconfig"

SYSTEM_HEADER_SEARCH_PATHS = $(inherited) $(SYSTEM_HEADER_SEARCH_PATHS_libiconv_libiconv)
GCC_PREPROCESSOR_DEFINITIONS = $(inherited) $(GCC_PREPROCESSOR_DEFINITIONS_libiconv_libiconv)
OTHER_CFLAGS = $(inherited) $(OTHER_CFLAGS_libiconv_libiconv)
OTHER_CPLUSPLUSFLAGS = $(inherited) $(OTHER_CPLUSPLUSFLAGS_libiconv_libiconv)
FRAMEWORK_SEARCH_PATHS = $(inherited) $(FRAMEWORK_SEARCH_PATHS_libiconv_libiconv)

// Link options for libiconv_libiconv
LIBRARY_SEARCH_PATHS = $(inherited) $(LIBRARY_SEARCH_PATHS_libiconv_libiconv)
OTHER_LDFLAGS = $(inherited) $(OTHER_LDFLAGS_libiconv_libiconv)