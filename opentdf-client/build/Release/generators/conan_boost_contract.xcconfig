// Conan XcodeDeps generated file for boost::contract
// Includes all configurations for each dependency

#include "conan_boost_contract_release_arm64.xcconfig"

SYSTEM_HEADER_SEARCH_PATHS = $(inherited) $(SYSTEM_HEADER_SEARCH_PATHS_boost_contract)
GCC_PREPROCESSOR_DEFINITIONS = $(inherited) $(GCC_PREPROCESSOR_DEFINITIONS_boost_contract)
OTHER_CFLAGS = $(inherited) $(OTHER_CFLAGS_boost_contract)
OTHER_CPLUSPLUSFLAGS = $(inherited) $(OTHER_CPLUSPLUSFLAGS_boost_contract)
FRAMEWORK_SEARCH_PATHS = $(inherited) $(FRAMEWORK_SEARCH_PATHS_boost_contract)

// Link options for boost_contract
LIBRARY_SEARCH_PATHS = $(inherited) $(LIBRARY_SEARCH_PATHS_boost_contract)
OTHER_LDFLAGS = $(inherited) $(OTHER_LDFLAGS_boost_contract)