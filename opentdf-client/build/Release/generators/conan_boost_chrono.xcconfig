// Conan XcodeDeps generated file for boost::chrono
// Includes all configurations for each dependency

#include "conan_boost_chrono_release_arm64.xcconfig"

SYSTEM_HEADER_SEARCH_PATHS = $(inherited) $(SYSTEM_HEADER_SEARCH_PATHS_boost_chrono)
GCC_PREPROCESSOR_DEFINITIONS = $(inherited) $(GCC_PREPROCESSOR_DEFINITIONS_boost_chrono)
OTHER_CFLAGS = $(inherited) $(OTHER_CFLAGS_boost_chrono)
OTHER_CPLUSPLUSFLAGS = $(inherited) $(OTHER_CPLUSPLUSFLAGS_boost_chrono)
FRAMEWORK_SEARCH_PATHS = $(inherited) $(FRAMEWORK_SEARCH_PATHS_boost_chrono)

// Link options for boost_chrono
LIBRARY_SEARCH_PATHS = $(inherited) $(LIBRARY_SEARCH_PATHS_boost_chrono)
OTHER_LDFLAGS = $(inherited) $(OTHER_LDFLAGS_boost_chrono)